{
  "name": "pahamin",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"pnpm --filter @pahamin/web dev\" \"pnpm --filter @pahamin/api start:dev\"",
    "dev:web": "pnpm --filter @pahamin/web dev",
    "dev:api": "pnpm --filter @pahamin/api start:dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
