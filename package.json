{
  "name": "pahamin",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "pnpm --filter @pahamin/web dev",
    "dev:web": "pnpm --filter @pahamin/web dev",
    "build:web": "pnpm --filter @pahamin/web build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
